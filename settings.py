# sqlite database name. place in project root folder.
db = 'expenses.db'

# available company credit cards
cards = ['CA (Spark VISA 1086)', 'BP (Spark VISA 1086)',
         'RX (VISA 3715)', 'Company Account']
